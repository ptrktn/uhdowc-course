NB: example-frontend build fails with ubuntu:18.04, an effective
    workaround is to use ubuntu:latest image.

Before:

example-backend 651MB
example-frontend 878MB

After:

example-backend 225MB
example-frontend 184MB
